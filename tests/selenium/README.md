# Selenium Tests

## Prerequisites
- Install Python dependencies:
  - `pip3 install selenium`
- Start the frontend locally:
  - `cd frontend`
  - `npm run dev`

## Run the test
From the project root:

```bash
python3 tests/selenium/test_homepage.py

This test verifies that the LiveLens homepage can be loaded successfully in a brower using Selenium